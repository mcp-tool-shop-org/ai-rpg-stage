# Godot 4 — editor-generated import cache and derived data.
.godot/
/android/

# Export artefacts. Builds are produced from source, never committed.
/build/
export_presets.cfg

# OS noise
.DS_Store
Thumbs.db
desktop.ini

# Scratch. If a file here matters, it belongs in the repo under a real name.
/scratch/
*.tmp

# Failed dimetric plates (wrong camera / no alpha). Evidence, not product.
assets/iso/*.png
assets/dimetric/_rejected/
*.blend1
